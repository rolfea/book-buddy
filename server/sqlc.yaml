version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/queries/"
    schema: "migrations/"
    gen:
      go:
        package: "query"
        out: "internal/data/query"
        emit_json_tags: true
        emit_prepared_queries: false
